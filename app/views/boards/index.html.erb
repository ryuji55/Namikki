<div class="container">
  <div class="row">
    <div class="col-lg-10 offset-lg-1">
      <h1 class="text-center">Namikki</h4>
    </div>
  </div>

  <div class="row">
     <div class="col-100">
        <div class="row scroll-list jscroll">
          <% if @boards.present? %>
            <%= render @boards %>
          <% else %>
            <p>投稿はありません</p>
          <% end %>
      </div>
      <%= paginate @boards %>
    </div>
  </div>
</div>
