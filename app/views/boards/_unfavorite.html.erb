<%= link_to board_favorite_path(id: current_user.id, board_id: board.id), id: "js-favorite-for-board-#{@favorite.id}", class:"float-right", method: :delete, remote: true do %>
  ♥<%= board.favorites.count %> いいね
<% end %>
