<div class="container">
  <div class="row">
    <% lists.each do |list| %>
      <%= list["weather"][0]["main"] %>
      <%= list["weather"][0]["description"] %>
      <%= list["dt_txt"] %>
      <%= image_tag "http://openweathermap.org/img/w/#{list["icon"]}.png" %>
    <% end %>
  </div>
</div>
